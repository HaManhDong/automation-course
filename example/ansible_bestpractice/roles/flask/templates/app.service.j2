[Unit]
Description=Flask web application
After=network.target

[Service]
User=root
WorkingDirectory="{{ deploy_folder }}/flask"
ExecStart="{{ venv_folder }}/bin/flask run --port {{ flask_port }}"
Restart=always

[Install]
WantedBy=multi-user.target